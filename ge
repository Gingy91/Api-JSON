# Перейти в папку с проектом (если еще не там)
cd /путь/к/проекту

# Инициализировать репозиторий (если еще не инициализирован)
git init

# Добавить все файлы в коммит
git add .

# Создать коммит с сообщением
git commit -m "Initial commit: Task API"

# Подключить удаленный репозиторий (замени ссылку на свою)
git remote add origin https://github.com/ТВОЙ_GITHUB_USERNAME/task-api.git

# Отправить код в репозиторий
git push -u origin main
